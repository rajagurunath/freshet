name: Deploy landing page (GitHub Pages)

# Publishes the static marketing site in apps/web to GitHub Pages.
# Self-enabling: configure-pages sets the source to "GitHub Actions" on first
# run (enablement: true). If your org disables API enablement, set it manually:
# repo Settings -> Pages -> Source = "GitHub Actions".
# Site URL: https://<owner>.github.io/<repo>/  (e.g. https://rajagurunath.github.io/freshet/)
on:
  push:
    branches: [main]
    paths:
      - "apps/web/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; don't cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # Enable Pages via the API on first run so no manual
          # Settings -> Pages toggle is required.
          enablement: true
      - name: Upload site (apps/web)
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web
      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
