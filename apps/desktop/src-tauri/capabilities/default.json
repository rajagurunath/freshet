{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities — read-only access to AI session directories.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "os:allow-platform",
    "os:allow-os-type",
    "fs:allow-read-file",
    "fs:allow-read-text-file",
    "fs:allow-read-dir",
    "fs:allow-exists",
    {
      "comment": "Read scope covers session roots plus the asset-hub scan directories: $HOME/.claude/{skills,commands,agents}/** (listed explicitly for clarity even though $HOME/.claude/** already includes them).",
      "identifier": "fs:scope",
      "allow": [
        { "path": "$HOME/.claude/**" },
        { "path": "$HOME/.claude/skills/**" },
        { "path": "$HOME/.claude/commands/**" },
        { "path": "$HOME/.claude/agents/**" },
        { "path": "$HOME/.codex/**" },
        { "path": "$HOME/Library/Application Support/Code/User/globalStorage/kilocode.kilo-code/**" },
        { "path": "$HOME/.config/Code/User/globalStorage/kilocode.kilo-code/**" }
      ]
    },
    {
      "comment": "Branch-from-turn writes new Claude Code session files. Scoped to the Claude projects tree only — branching never modifies any other file.",
      "identifier": "fs:allow-write-text-file",
      "allow": [{ "path": "$HOME/.claude/projects/**" }]
    }
  ]
}
