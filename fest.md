0. Need a landing page that describes the usefulness of the features in a very simple and straightforward way, like any other Y Combinator-inspired UI plot code design /playground or /frontend-design etc.
    Annimation showing the protocol interactions and each usecases should be there on the landing page.
1. User can use the context hub, go into a session, and branch out from a particular conversation or particular turn and start a new conversation. 
2. Better display of the sessions Sorting by different options like date, project, tool,cost,tokens  etc.
3. Auto-sync mode: user can opt in to have all sessions automatically pushed to the hub Using batch jobs,Since a batch job is cheaper than the online job, we can submit a batch job and summarize the sessions. Once it is done by the OpenAI cloud APIs, we will edit that event and fetch the summary. This is one way. Another way can be using a local model if the system permits, if the system has enough resources. 
4. Better search and retrieval: we can use better vector databases, or we can use a hybrid approach of keyword search + vector search.
5. Ways to collect harness, like if there is a custom script or custom skills from different employees into a centralized context hub. We need to categorize these things and then collect and keep them in the context hub as reusable assets. 
    5.1 https://github.com/OpenSharing-IO/OpenSharing So, this is a new project by Databricks. Can we leverage this to share between agents and the central hub? Please take a look and explore how this protocol works, and we can adapt to this protocol for our sharing. 
6. Similar to linear tickets, if getting displayed on the PR descriptions or PR comments in GitHub, I want to have a link which displays the context why this particular PR was raised and what is the thought process the agent went through to raise this PR and what are the comments. All these things, if just by clicking the link, the developer can have all this context. 
   6.1 So, the agent can push the code and also push the context to the context hub, and this can be pulled in real time and then it can be shared in the PR link. 
7. We can have a better UI for the desktop app, which can display the sessions in a more user-friendly way, and also we can have a better way to summarize the sessions, like we can have a summary view which can display the summary of the session and also we can have a detailed view which can display the entire session.
8. How to show the compact version. Let's say the user runs /compact; the session might have compacted, so can we use the compact version for summary? Can we highlight the compacted version also in the session? I want a filter for sessions which have run compact in the session. 
9. Connect the context or knowledge between sessions as a knowledge graph. The knowledge graph should be able to connect relations like multiple microservices connected in a single feature as a relation, for example, and the marketing effort for a particular feature combined with the feature implementation in the microservice repo. Those things have to be linked by a graph. This is a little sophisticated one; we need to plan this one, and based on the context of the session, each session has to be connected via a graph. And also extract the information as nodes and a edges so that this can be easily scanned, like basically a graphRag. 
10. Extract small common things among all the sessions, for example, commit style and some preference by the users across all the sessions, all the repositories, and all the repos. We can standardize those as rules. This is kind of a rules extraction from their sessions automatically, and then it asks you if this rule is valid or not. Based on the consent, you can proceed with the standardization of the rules. 
11. If the weekly subscription is ending for cloud, for example, the context hub can detect this. Before the window ends, it can use those windows of cloud or codex for summarization or for graph knowledge formation, etc., so each week will use the subscription fully. 